<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Signal.Resources.Templates"
    xmlns:base="using:Signal">

    <!--<Style TargetType="local:PreferenceCategory" xmlns:local="using:Signal.Resources.Templates">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border>
                        <ItemsPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Background" Value="Green"/>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid Background="Green">
                        <ContentPresenter Margin="5"/>
                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ContentPresenter">
                    <Setter Property="ContentTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <Grid>
                                    <ContentPresenter/>
                                </Grid>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Background" Value="Green"/>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>-->
    <Style TargetType="local:SwitchPreference" xmlns:local="using:Signal.Resources.Templates">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:SwitchPreference">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">

                        <StackPanel>
                            <TextBlock 
                            Text="{TemplateBinding Title}"
                            Style="{ThemeResource TitleTextBlockStyle}"/>

                            <TextBlock 
                            Text="{TemplateBinding Text}"
                            Style="{ThemeResource BodyTextBlockStyle}"/>

                            <ToggleSwitch IsOn="{TemplateBinding IsOn}"/>
                        </StackPanel>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:ListPreference" xmlns:local="using:Signal.Resources.Templates">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate x:Name="base" TargetType="local:ListPreference">
                    <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <StackPanel x:Name="ControlStackPanel">
                            <TextBlock 
                            Text="{TemplateBinding Title}"
                            Style="{ThemeResource TitleTextBlockStyle}"/>

                            <TextBlock 
                            Text="{TemplateBinding Text}"
                            Style="{ThemeResource BodyTextBlockStyle}"/>
                            <!--ItemsSource="{TemplateBinding Items}"  SelectedItem="{Binding SelectedItem, Converter={StaticResource RadioConverter}, ConverterParameter={RelativeSource Self}, Mode=TwoWay}"-->
                            <ListView x:Name="list"   SelectionMode="Single">
                                <ListView.Resources>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListViewItem" x:Name="item">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>

                                                        <RadioButton IsChecked="{Binding IsSelected, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"/>
                                                        <ContentPresenter Grid.Column="1"/>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="IsSelected" Value=""/>
                                    </Style>
                                </ListView.Resources>
                                <ListView.Items>
                                    <ListViewItem IsSelected="{Binding Path=SelectedValue, ElementName=base, Mode=TwoWay, Converter={StaticResource RadioConverter}, ConverterParameter={Binding IsSelected}}">Test1</ListViewItem>
                                    <!--<ListViewItem IsSelected="{TemplateBinding }">Test2</ListViewItem>-->
                                    <ListViewItem IsSelected="{Binding Path=SelectedValue, ElementName=base, Mode=TwoWay}">Test3</ListViewItem>

                                </ListView.Items>
                            </ListView>

                            <TextBlock 
                                
                            Text="{TemplateBinding SelectedValue}"
                            Style="{ThemeResource BodyTextBlockStyle}"/>

                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:PreferenceBase" xmlns:local="using:Signal.Resources.Templates">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:PreferenceBase">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:SettingsCategory" xmlns:local="using:Signal.Resources.Templates">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:SettingsCategory">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid Margin="0,4,0,4">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Viewbox Grid.RowSpan="2"
                                Width="40"
                                Height="40" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <SymbolIcon
                                    Margin="4, 4, 4, 4"
                                    Symbol="{TemplateBinding Icon}" 
                                    Foreground="{ThemeResource SystemControlBackgroundAccentBrush}"/>
                            </Viewbox>
                            <TextBlock
                                Grid.Column="1"
                                MaxLines="1"
                                Margin="0"
                                Text="{TemplateBinding Title}"
                                TextWrapping="NoWrap"
                                Style="{StaticResource BodyTextBlockStyle}"/>

                            <TextBlock
                                Grid.Column="1"
                                Grid.Row="1"
                                Margin="0"
                                Text="{TemplateBinding Description}"
                                TextWrapping="NoWrap"
                                Style="{StaticResource BodyTextBlockStyle}"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <!--<Setter Property="Background" Value="Green"/>-->
        <!--<Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid Background="Green">
                        <ContentPresenter Margin="5"/>
                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>-->
    </Style>

    <Style TargetType="local:ButtonPreference" xmlns:local="using:Signal.Resources.Templates">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ButtonPreference">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">

                        <StackPanel>
                            <TextBlock 
                            Text="{TemplateBinding Title}"
                            Style="{ThemeResource TitleTextBlockStyle}"/>

                            <TextBlock 
                            Text="{TemplateBinding Text}"
                            Style="{ThemeResource BodyTextBlockStyle}"/>

                            <Button Content="{TemplateBinding ButtonText}" Command="{TemplateBinding ButtonCommand}"/>
                        </StackPanel>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <base:DataTemplates x:Key="test"/>
    
    
    <!--<Style TargetType="local:SettingsPage" xmlns:local="using:Signal.Resources.Templates">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:SettingsPage">
                    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="AdaptiveStates" CurrentStateChanged="AdaptiveStates_CurrentStateChanged">
                                <VisualState x:Name="DefaultState">
                                    <VisualState.StateTriggers>
                                        <AdaptiveTrigger MinWindowWidth="720" />
                                    </VisualState.StateTriggers>
                                </VisualState>

                                <VisualState x:Name="NarrowState">
                                    <VisualState.StateTriggers>
                                        <AdaptiveTrigger MinWindowWidth="0" />
                                    </VisualState.StateTriggers>

                                    <VisualState.Setters>
                                        <Setter Target="MasterColumn.Width" Value="*" />
                                        <Setter Target="DetailColumn.Width" Value="0" />
                                        <Setter Target="MasterListView.SelectionMode" Value="None" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition x:Name="MasterColumn" Width="320" />
                            <ColumnDefinition x:Name="DetailColumn" Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel 
                            Padding="12,4,12,4" 
                            Grid.ColumnSpan="2" 
                            Orientation="Horizontal" 
                            Background="{ThemeResource SystemControlPageBackgroundListLowBrush}">
                            
                            <Viewbox Width="24" Height="24">
                                <SymbolIcon
                                    Symbol="Setting" 
                                    Foreground="{ThemeResource SystemControlBackgroundAccentBrush}"/>
                            </Viewbox>
                        </StackPanel>
                        
                        <TextBlock
                            Text="SETTINGS"
                            Margin="16,8,8,8"
                            Style="{ThemeResource BaseTextBlockStyle}"
                            VerticalAlignment="Center" />

                        <ListView
                            x:Name="MasterListView"
                            Grid.Row="2"
                            Background="{ThemeResource SystemControlPageBackgroundListLowBrush}"
                            ItemContainerTransitions="{x:Null}"
                            ItemsSource="{TemplateBinding Categories}"
                            ItemTemplate="{StaticResource SettingsCategoryListViewItemTemplate}">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>

                        <ContentPresenter
                            Background="Red"
                            Grid.Column="1"
                            Grid.Row="1"
                            Grid.RowSpan="2"
                            BorderThickness="1,0,0,0"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
</ResourceDictionary>
